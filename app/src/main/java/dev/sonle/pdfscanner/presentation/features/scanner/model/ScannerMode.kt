package dev.sonle.pdfscanner.presentation.features.scanner.model

/**
 * Capture mode for the scanner camera.
 */
enum class ScannerMode {
    /** Automatically captures when document is stable */
    AUTO,
    /** User must tap the shutter button to capture */
    MANUAL
}
